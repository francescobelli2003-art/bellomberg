# Contributing

Bellomberg is developed in a private repository and published through an allowlisted export. Accepted changes are applied upstream and appear in a later sync commit; a pull request may therefore be closed with a reference to the applied commit instead of being merged directly. Published history is not force-pushed.

Questions go to GitHub Discussions (see [SUPPORT.md](SUPPORT.md)). Security problems go through private reporting (see [SECURITY.md](SECURITY.md)), never a public issue. Participation is covered by the [code of conduct](CODE_OF_CONDUCT.md).

## Useful contributions

- A reproducible bug report with synthetic inputs and the exact observed result.
- A focused failing test that demonstrates the issue offline.
- A small change that preserves existing interfaces unless the interface itself is the defect.
- Documentation corrections tied to current package paths and commands.

Never submit API keys, tokens, personal financial records, real mandates, absolute personal paths, or production database extracts.

## Repository layout

- `src/bellomberg/` is the installable Python package: `api/`, `agents/`, `core/`, `storage/`, `market_data/`, `valuation/`, `portfolio/`, `reporting/`, `cli/` and `resources/examples/`. [docs/ARCHITETTURA.md](docs/ARCHITETTURA.md) describes each area.
- `app/` is the Electron/React desktop: `app/src/` (pages, components, hooks, pure helpers), `app/electron/` (window and backend lifecycle), `app/tests/` (release contracts, product tests, desktop smoke test).
- `tools/` holds operator utilities (`tools/ops/`), explicit data and schema migrations (`tools/migrations/`) and the release export with its gates (`tools/release/`).
- `tests/` is the offline pytest suite; `pytest.ini` restricts collection to this directory.
- `docs/` holds the public handbook (`docs/guide/`), the architecture notes and setup notes. The illustrations in `docs/assets/product/` are synthetic and generated by `docs/guide/build_mockups.py`.
- The `*.py` and `*.bat` files at the root are thin launchers kept for operational compatibility. New logic belongs in the package, not in a launcher.

## Development setup

Use Python 3.10 or newer and Node.js 22.12 or newer. CI runs Python 3.12.

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install -e . pytest
Copy-Item .env.example .env
cd app
npm ci
npm run desktop:install
cd ..
$env:BELLOMBERG_PYTHON = (Resolve-Path .venv\Scripts\python.exe).Path
```

Use synthetic provider credentials and temporary databases in tests. Do not call paid APIs or a real broker from the offline suite.

## Project rules

Financial and research code follows three core rules:

1. Missing, invalid, or stale data is declared. Do not replace it with a silent default or proxy; a proxy that must remain is labeled as such.
2. Numeric claims produced by agents must trace back to tool output and carry a `[src: tool]` citation.
3. SQLite is the only portfolio source. Do not introduce a parallel spreadsheet or JSON portfolio.

Every contribution follows these:

4. **No secrets and no personal data**, anywhere: code, tests, fixtures, documentation, screenshots, commit messages. Instruments and values in examples and tests are invented.
5. **Surgical edits.** Change the smallest thing that fixes the trigger. Do not rewrite a module, reformat a file you are not changing, or move code without a reason stated in the pull request.
6. **Tests first.** Write the failing test, read the failure line to confirm it fails for the intended reason, then change the code. Do not weaken an assertion or add a skip to hide a failure; a skip states its reason. Tests assert what a user can observe, not implementation details.
7. **Interface text.** The interface currently uses Italian labels and a dictionary layer under `app/src/i18n/` is being introduced for user-facing text. Add new user-facing strings through that layer when it exists for the page you touch; until then keep the label style of the surrounding page and list the new strings in the pull request so they can be moved. Do not add a parallel translation mechanism.
8. **Language.** Source comments keep the language of the surrounding file. Public documentation (README, `docs/`, community files) is written in English.
9. **Protected decisions.** Model identifiers, the financial policies (the action validator flags, it does not block) and the valuation gate (a fair value only from documented records, otherwise `FV n.d.`) are not changed without the maintainer's explicit agreement.

Windows is the supported desktop platform; preserve CRLF where `.gitattributes` requires it and use portable path APIs in Python.

## Tests

Run the smallest relevant battery first, then the offline suite:

```powershell
python -m pytest tests/test_<battery>.py -q
python -m pytest tests/ -q
```

For frontend changes:

```powershell
cd app
npx tsc --noEmit
npm run test:release
npm run build:bundles
npm run test:desktop
```

For documentation changes, `python docs/guide/verify_docs.py` checks handbook links, the page inventory and the synthetic SVG assets.

State which checks you ran and which live integrations remain untested. A passing offline suite does not validate current provider availability, account entitlements, brokerage connectivity, or email delivery.

## Pull requests

Fill in the pull request template: the concrete trigger, the previous behavior, the resulting behavior, and the validation you ran. Keep unrelated cleanup separate. New dependencies need a clear reason and should avoid unnecessary major-version changes.

List every **new file** in the pull request. The export allowlist is private; a file that is not added to it does not reach the public repository. Add a line to `CHANGELOG.md` under `[Unreleased]` when the change is visible to users.

By contributing, you agree that your contribution may be included under the repository's Apache-2.0 license. See `NOTICE` for project notices.
