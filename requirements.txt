requests>=2.31.0
curl_cffi>=0.7  # 27/08: strategy.com (mNAV MSTR, Akamai) vuole il SET di header di un browser: curl_cffi lo manda e lo mantiene (era gia' dipendenza di yfinance)
feedparser>=6.0.10
python-dotenv>=1.0.0
yfinance>=0.2.54  # review 17/07: <0.2.54 dava dividendYield in FRAZIONI, il canale peer banca assume unita' percento (misura 17/07)
openpyxl>=3.1.0
ib_async>=1.0.1
matplotlib>=3.8.0
reportlab>=4.0.0
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.0.0
statsmodels>=0.14.0
arch>=6.3.0
squarify>=0.4.4
chromadb>=0.4.20
fastapi>=0.115.0
starlette>=0.46.0  # API middleware/JSON response: import diretto, range compatibile con FastAPI verificato
uvicorn[standard]>=0.32.0
psutil>=5.9.0  # quick-win audit/21: import lazy in bellomberg_api (scan zombie 8765), era fuori lista
pypdf>=4.0.0  # 02/09 (B7): lettore_trimestrali._estrai_pdf lo importa dentro un try che diceva «illeggibile» a ogni PDF senza il pacchetto - CI rossa dal 31/08
beautifulsoup4>=4.14.3  # valuation source HTML parser; explicit dependency tested locally
pydantic>=2.0  # 02/09 (B7): importato direttamente (modelli degli endpoint); arrivava solo come dipendenza transitiva di fastapi
httpx>=0.27  # 05/09: llm_client.py (OpenRouter) lo importa direttamente; prima arrivava solo come dipendenza transitiva di anthropic (stessa classe di pypdf/pydantic del 02/09)
